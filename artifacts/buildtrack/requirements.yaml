packages:
  "recharts": "For rendering the dashboard analytics charts (pie, bar, line)"
  "date-fns": "For formatting dates in a readable format"
  "clsx": "For conditional class names"
  "tailwind-merge": "For merging tailwind classes safely"
  "react-hook-form": "For robust form state management"
  "@hookform/resolvers": "For Zod schema validation integration with react-hook-form"

images:
  - path: "artifacts/buildtrack/public/images/auth-bg.png"
    description: "Rugged construction site background, architectural blueprints overlay, dark slate and orange lighting, dramatic shadows, professional and industrial feel"
    aspect_ratio: "16:9"

notes:
  - Uses @workspace/api-client-react for all API interactions
  - Uses cookie-backed session auth via /api/auth/user for authentication state

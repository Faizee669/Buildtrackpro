{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": [
      "artifacts/api-server/**",
      "lib/**"
    ]
  },
  "deploy": {
    "startCommand": "cd artifacts/api-server && pnpm run dev",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
